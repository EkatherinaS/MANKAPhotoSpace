@model IEnumerable<PracticalTraining.Models.DatabaseMANKA.PaymentInfo>

@section menu
{
    <li class="nav-item">
        <a class="nav-link text-dark" asp-area="" asp-controller="Owner" asp-action="Index">На главную</a>
    </li>
    <li class="nav-item">
        <a class="nav-link text-dark" asp-area="" asp-controller="Home" asp-action="Index">Выйти</a>
    </li>
}

<h1>Способы оплаты</h1>

<p>
    <a asp-action="Create"><text>Добавить</text></a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                <text>Название способа оплаты</text>
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PaymentName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PaymentCode">Редактировать</a> |
                <a asp-action="Delete" asp-route-id="@item.PaymentCode">Удалить</a>
            </td>
        </tr>
}
    </tbody>
</table>
